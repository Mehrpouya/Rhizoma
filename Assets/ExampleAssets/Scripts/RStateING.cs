using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RStateING : MonoBehaviour
{
    //This is an incredible state. If you happen to ever experience the ING state, you should consider yourself a success. 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
